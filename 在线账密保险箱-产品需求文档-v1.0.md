# 在线账密保险箱产品需求文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 文档状态 | 需求初稿 |
| 编写日期 | 2026-07-20 |
| 产品形态 | 多用户在线账密管理网站 |

## 1. 项目背景

用户从不同平台、商家或其他渠道获得账号时，拿到的资料并不统一。除常见的登录账号和密码外，还可能包含辅助邮箱、邮箱密码、2FA 密钥、验证码获取地址、接码平台、兑换码、到期时间、使用教程及其他说明。

使用固定表格或普通文档保存这些信息，存在以下问题：

- 字段固定，无法适应不同平台和渠道。
- 明文保存账号、密码和 2FA 密钥，泄露风险较高。
- 多设备之间难以安全同步。
- 账号数量增加后，搜索、分类和维护困难。
- 多人使用时，无法保证不同用户之间的数据隔离。

因此，需要建设一个支持多人独立注册、在线访问、灵活录入和加密保存的账密保险箱网站。

## 2. 产品目标

### 2.1 核心目标

- 支持多个个人用户分别注册和登录。
- 每个用户只能访问和管理自己的账密数据。
- 支持用户根据实际拿到的资料，自由增加、删除和排列字段。
- 支持按照平台和获取渠道建立可重复使用的录入模板。
- 支持用户在电脑和手机浏览器中访问同一份数据。
- 对账号、密码、2FA 密钥、备注等内容进行客户端加密。
- 管理员可以管理网站和用户，但不能查看用户的账密明文。

### 2.2 第一版非目标

以下内容暂不纳入第一版：

- 浏览器自动填充插件。
- 自动登录第三方网站。
- 从商家或第三方平台自动抓取账号。
- 用户之间共享或转让账密。
- 团队协作和复杂组织权限。
- 收费、套餐、订单及支付系统。
- 原生 iOS、Android 或桌面客户端。

## 3. 用户角色与权限

### 3.1 个人用户

个人用户可以：

- 注册和登录网站。
- 创建并解锁自己的保险箱。
- 新增、查看、修改和删除自己的账密记录。
- 创建和使用私人渠道模板。
- 使用管理员提供的系统模板。
- 搜索、筛选和分类自己的记录。
- 导出自己的加密备份。
- 管理自己的登录设备和安全设置。
- 注销自己的账户。

个人用户不能：

- 查看其他用户是否保存了某个平台的账号。
- 查看、修改或删除其他用户的数据。
- 访问管理后台。
- 创建或修改系统模板。

### 3.2 管理员

管理员可以：

- 通过独立的管理员入口登录。
- 查看用户列表和用户状态。
- 启用、禁用或解除用户登录会话。
- 管理注册策略和邀请码。
- 创建、修改和停用系统模板。
- 发布系统公告。
- 查看不包含账密内容的安全日志。
- 查看用户数量、存储用量和服务运行状态。
- 执行数据库密文备份和系统维护。

管理员不能：

- 查看用户保存的账号、密码、2FA 密钥、备注等明文内容。
- 代替用户解锁保险箱。
- 获取用户的保险箱主密码或恢复密钥。
- 将某个用户的账密迁移给其他用户。

## 4. 核心业务流程

### 4.1 个人用户首次使用

1. 用户进入个人用户注册页面。
2. 用户填写手机号、用户名、网站登录密码和确认密码。
3. 系统校验手机号和用户名未被注册，并校验两次密码一致。
4. 第一版不发送短信验证码；启用邀请码注册时，用户还需完成邀请码校验。
5. 用户首次登录后设置保险箱主密码。
6. 系统在浏览器内生成加密参数和恢复密钥。
7. 用户下载或抄写恢复密钥，并确认已妥善保存。
8. 用户进入空的保险箱首页。
9. 用户手动新增第一条账密记录。

### 4.2 日常登录与使用

1. 用户通过网站登录密码登录。
2. 用户输入保险箱主密码解锁数据。
3. 浏览器获取该用户的密文数据并在本地解密。
4. 用户进行查看、搜索、复制、新增或编辑操作。
5. 数据在浏览器中重新加密后上传到服务器。
6. 用户退出登录、主动锁定或长时间无操作后，保险箱清除内存中的解密数据。

### 4.3 管理员登录

1. 管理员进入独立的管理员登录页面。
2. 管理员输入管理员账号和密码。
3. 管理员完成强制二次验证。
4. 系统校验管理员角色后进入管理后台。
5. 管理员只能查看管理数据和安全元数据，不能读取用户保险箱内容。

## 5. 页面与导航

### 5.1 公共页面

| 页面 | 建议路由 | 主要功能 |
| --- | --- | --- |
| 入口页 | `/` | 展示个人用户登录和管理员登录入口 |
| 个人用户登录 | `/login` | 用户登录、忘记登录密码、前往注册 |
| 个人用户注册 | `/register` | 创建个人用户账户 |
| 管理员登录 | `/admin/login` | 管理员专用登录和二次验证 |
| 找回登录密码 | `/forgot-password` | 重置网站登录密码 |

入口页需要明显区分“个人用户登录”和“管理员登录”。两类入口使用不同路由、会话校验和权限守卫，不能只依赖前端按钮隐藏来区分权限。

### 5.2 个人用户页面

| 页面 | 建议路由 | 主要功能 |
| --- | --- | --- |
| 保险箱首页 | `/vault` | 记录列表、搜索、筛选和快速操作 |
| 新增记录 | `/vault/new` | 手动录入动态字段 |
| 记录详情 | `/vault/items/:id` | 查看、复制及管理单条记录 |
| 编辑记录 | `/vault/items/:id/edit` | 修改记录及动态字段 |
| 模板管理 | `/vault/templates` | 管理用户的私人渠道模板 |
| 安全设置 | `/settings/security` | 修改密码、查看设备、自动锁定设置 |
| 备份与恢复 | `/settings/backup` | 导出和导入加密备份 |

### 5.3 管理员页面

| 页面 | 建议路由 | 主要功能 |
| --- | --- | --- |
| 管理概览 | `/admin` | 用户数量、系统状态及安全提醒 |
| 用户管理 | `/admin/users` | 查询、启用和禁用用户 |
| 邀请码管理 | `/admin/invitations` | 创建、停用和查看邀请码状态 |
| 系统模板 | `/admin/templates` | 管理所有用户可用的模板 |
| 安全日志 | `/admin/security-logs` | 查看登录和管理操作日志 |
| 系统设置 | `/admin/settings` | 注册策略、公告和安全策略 |

## 6. 账密记录模型

### 6.1 固定基础信息

每条记录包含以下基础信息：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 记录名称 | 是 | 用户识别该记录的名称 |
| 所属平台 | 是 | 例如 ChatGPT、Claude、Google |
| 获取渠道 | 否 | 例如商家、自行注册、他人提供 |
| 使用模板 | 否 | 创建记录时采用的模板 |
| 状态 | 是 | 正常、待验证、异常、失效、已归档 |
| 标签 | 否 | 支持多个自定义标签 |
| 创建时间 | 自动 | 系统生成 |
| 更新时间 | 自动 | 系统生成 |

### 6.2 动态字段

除基础信息外，其余信息全部由动态字段承载。第一版支持以下字段类型：

| 字段类型 | 示例 | 默认行为 |
| --- | --- | --- |
| 普通文本 | 用户名、渠道名称 | 正常显示，可复制 |
| 密码/密钥 | 登录密码、API 密钥 | 默认隐藏，可显示和复制 |
| 邮箱 | 登录邮箱、辅助邮箱 | 可复制 |
| 手机号 | 绑定手机、接码号码 | 可复制 |
| 网址 | 验证码入口、登录教程 | 可复制和打开 |
| 2FA 密钥 | TOTP 种子 | 默认隐藏，可生成动态验证码 |
| 验证码/兑换码 | 接码兑换码、激活码 | 默认隐藏，可复制 |
| 日期时间 | 到期时间、购买时间 | 支持日期选择和到期提示 |
| 多行文本 | 使用说明、异常处理说明 | 支持较长文本 |
| 单选项 | 账号类型、地区 | 从预设选项中选择 |

每个动态字段包含以下配置：

- 字段名称。
- 字段类型。
- 字段值。
- 是否必填。
- 是否按敏感信息隐藏。
- 是否显示复制按钮。
- 字段说明或占位提示。
- 排列顺序。

用户在编辑记录时可以：

- 添加任意数量的动态字段。
- 修改字段名称和类型。
- 拖拽调整字段顺序。
- 删除不需要的字段。
- 将普通字段切换为敏感字段。

### 6.3 数据结构原则

- 不为每种渠道创建不同的数据表。
- 动态字段以可版本化的结构保存，并作为账密密文的一部分。
- 每条记录保存创建时使用的模板快照。
- 后续修改模板时，不自动破坏或覆盖已有记录。
- 用户可以主动选择用新版模板补充字段，但不得覆盖已有字段值。

## 7. 平台、渠道与模板

### 7.1 概念区分

- 平台：账号实际使用的服务，例如 ChatGPT、Claude、Google。
- 获取渠道：用户取得账号资料的来源，例如某个商家、自行注册或他人提供。
- 模板：某个平台和渠道常用的字段组合。

同一个平台可以存在多个渠道，每个渠道可以拥有不同模板。

### 7.2 系统模板

- 由管理员创建和维护。
- 所有个人用户均可查看和使用。
- 模板只能定义字段结构、提示和顺序，不得包含真实账号或密码。
- 管理员可以停用模板，但不能因此删除用户已经创建的记录。

### 7.3 私人模板

- 由个人用户创建。
- 仅创建者本人可见。
- 用户可以从现有账密记录中保存模板。
- 保存模板时只保存字段名称和配置，不保存字段值。
- 用户可以复制系统模板后进行私人修改。

### 7.4 使用模板创建记录

1. 用户选择平台。
2. 用户选择获取渠道。
3. 系统列出匹配的系统模板和私人模板。
4. 用户选择模板后，页面生成对应字段。
5. 用户可以继续增加、修改或删除字段。
6. 用户填写内容并保存。

## 8. 账密录入能力

### 8.1 手动录入

手动录入是第一版的核心方式，必须支持：

- 从空白记录开始录入。
- 从系统模板开始录入。
- 从私人模板开始录入。
- 在录入过程中添加自定义字段。
- 保存当前字段结构为私人模板。
- 保存前检查必填字段。
- 离开未保存页面前进行提示。

### 8.2 文本粘贴识别

作为第一版增强功能，用户可以粘贴一段账密文本，系统尝试识别常见的“字段名 + 字段值”结构，例如：

```text
账号：example@example.com
密码：********
辅助邮箱：backup@example.com
```

识别要求：

- 自动生成候选字段，不直接保存。
- 在用户确认页面展示识别结果。
- 用户可以修改字段名称、类型和敏感属性。
- 无法识别的内容进入备注候选区。
- 原始文本和解析结果不得写入日志。

### 8.3 后续导入方式

以下能力作为后续迭代项：

- Markdown 文件导入。
- CSV 或 Excel 批量导入。
- JSON 加密备份导入。
- 多条账密批量粘贴。
- 重复账号检测和合并。

## 9. 保险箱首页与记录操作

### 9.1 列表展示

保险箱首页支持：

- 按平台、渠道、状态和标签筛选。
- 按记录名称和解密后的字段在本地搜索。
- 按更新时间、创建时间和到期时间排序。
- 卡片视图和列表视图。
- 显示记录名称、平台、渠道、状态和更新时间。
- 默认不在列表中展示密码、2FA 密钥和其他敏感字段。

### 9.2 详情操作

- 显示或隐藏敏感字段。
- 一键复制字段内容。
- 打开网址字段。
- 根据 2FA 密钥在浏览器本地生成动态验证码。
- 编辑、归档或删除记录。
- 查看记录的创建和更新时间。

复制敏感字段时，不在提示信息中显示被复制的实际内容。

### 9.3 删除与归档

- 普通删除先进入回收站。
- 回收站记录保留一定期限后永久删除，期限由系统配置。
- 用户可以手动执行永久删除，并需要二次确认。
- 已归档记录默认不出现在正常列表中。

## 10. 注册、登录与账户安全

### 10.1 注册策略

系统需要支持管理员配置以下模式：

- 关闭注册。
- 邀请码注册。
- 开放注册。

第一版默认采用邀请码注册，后续可以由管理员切换为开放注册。

注册页面包含以下字段：

| 字段 | 必填 | 第一版规则 |
|---|---:|---|
| 手机号 | 是 | 格式校验；标准化后全局唯一；第一版不发送短信验证码 |
| 用户名 | 是 | 全局唯一；用于识别用户，不能与已有用户名重复 |
| 密码 | 是 | 必须符合网站登录密码强度策略，只保存安全哈希 |
| 确认密码 | 是 | 必须与密码完全一致，仅用于前端和接口校验，不落库 |

注册接口还必须满足：

- 手机号和用户名都需要在数据库层建立唯一约束，不能只依赖前端校验。
- 手机号在比较和保存前进行标准化，避免空格、分隔符或国家区号写法造成重复账号。
- 第一版未验证手机号真实性，因此账户状态中应明确记录“手机号未验证”，不能把该手机号用于可信身份恢复。
- 注册请求需要进行频率限制；出现异常批量注册行为时可以启用图形验证码或临时拦截。
- 接口返回不能泄露过多已注册用户信息；手机号或用户名冲突时，仅返回完成注册所需的明确提示。

### 10.2 登录密码与保险箱主密码

系统区分两类密码：

- 网站登录密码：用于身份认证，只保存不可逆密码哈希；第一版不依赖短信验证码或邮箱完成重置。
- 保险箱主密码：用于在浏览器中派生加密密钥，服务器不得保存其明文或可逆形式。

重置网站登录密码不能自动解密保险箱，也不能替代保险箱主密码。

### 10.3 恢复密钥

- 首次创建保险箱时生成恢复密钥。
- 恢复密钥只向用户展示，并提示用户离线保存。
- 服务器不得保存可直接解密保险箱的恢复密钥明文。
- 用户忘记主密码时，可以使用恢复密钥重新建立保险箱访问能力。
- 用户同时丢失主密码和恢复密钥时，系统明确提示数据无法恢复。

### 10.4 会话与设备

- 支持查看当前登录设备。
- 支持退出指定设备或退出全部设备。
- 支持用户配置自动锁定时间。
- 页面关闭、退出登录或自动锁定后，清除浏览器内存中的明文数据。
- 登录失败达到阈值后执行限流或验证码校验。
- 管理员登录强制启用二次验证。

## 11. 加密与数据隔离要求

### 11.1 客户端加密

- 账密数据在浏览器中加密后再上传服务器。
- 账密数据从服务器下载后只在浏览器中解密。
- 服务端和管理员不能获得用户账密明文。
- 建议使用经过审查的密钥派生算法和带认证加密算法。
- 每个用户拥有独立的密钥材料和加密参数。
- 每条记录使用独立的随机数或等效安全机制，禁止重复使用加密随机数。

### 11.2 多用户隔离

- 所有用户数据查询必须绑定当前已认证用户身份。
- 后端不能信任前端传入的用户 ID。
- 用户不能通过修改请求参数访问其他用户的数据。
- 数据库访问、缓存、导出和备份均需维持租户隔离。
- 管理员后台接口与个人用户接口分别进行权限校验。

### 11.3 日志保护

以下内容禁止写入应用日志、访问日志、错误追踪或分析系统：

- 登录密码和保险箱主密码。
- 账号、密码和密钥字段值。
- 2FA 密钥和动态验证码。
- 恢复密钥。
- 用户粘贴的原始账密文本。
- 解密后的记录名称、备注和自定义字段。

### 11.4 传输与部署

- 正式环境必须使用 HTTPS。
- 使用安全 Cookie 保存登录会话。
- 管理后台启用更严格的登录限流。
- 关键配置和服务端密钥不得写入代码仓库。
- 数据库备份只能包含密文账密数据，并限制访问权限。
- 正式上线前进行越权访问、会话安全和前端注入测试。

## 12. 管理后台需求

### 12.1 用户管理

- 按手机号、用户名、状态和注册时间查询用户。
- 查看用户的注册时间、最近登录时间和账户状态。
- 启用、禁用用户。
- 使用户的现有登录会话失效。
- 查看用户占用的密文存储空间和记录数量。
- 不展示任何账密字段和解密内容。

### 12.2 邀请码管理

- 创建单次或多次使用的邀请码。
- 设置邀请码有效期和使用次数。
- 停用未使用完的邀请码。
- 查看邀请码创建者、使用状态和使用时间。
- 邀请码本身不授予管理员权限。

### 12.3 系统模板管理

- 创建平台和渠道。
- 创建、复制、编辑、排序和停用系统模板。
- 配置模板中的动态字段和提示。
- 预览用户录入时看到的表单。
- 模板中禁止写入真实账密示例。

### 12.4 安全日志

记录以下安全事件：

- 登录成功与失败。
- 密码重置请求。
- 二次验证变更。
- 新设备登录和设备退出。
- 管理员登录及管理操作。
- 用户启用、禁用和会话失效操作。

日志只保存必要的时间、事件类型、结果、用户标识和经过适当处理的网络信息，不保存账密内容。

## 13. 非功能需求

### 13.1 易用性

- 支持电脑和手机浏览器的响应式布局。
- 常用的显示、复制、新增和搜索操作应清晰可见。
- 敏感字段默认隐藏。
- 对不可恢复的操作提供明确说明和二次确认。
- 用户不需要理解加密原理即可完成日常使用。

### 13.2 性能

- 普通网络条件下，登录页和保险箱外壳应快速加载。
- 第一版应满足单个用户至少管理数千条记录。
- 搜索和筛选优先在已解密的本地数据中执行。
- 大量记录解密时应避免阻塞界面，并显示进度状态。

### 13.3 兼容性

- 支持主流桌面端 Chromium 浏览器。
- 支持主流 Android 和 iOS 浏览器。
- 不支持安全能力不足或停止维护的旧版浏览器。

### 13.4 可维护性

- 前端、个人用户接口和管理员接口保持清晰边界。
- 动态字段类型采用可扩展注册机制。
- 数据结构和模板结构包含版本号。
- 数据库变更使用可追踪的迁移机制。
- 核心加密、权限和数据隔离逻辑具备自动化测试。

## 14. 第一版范围与优先级

### 14.1 P0：必须完成

- 个人用户使用手机号、用户名、密码和确认密码完成注册，并使用网站登录密码登录。
- 第一版注册不发送短信验证码。
- 独立的管理员登录入口。
- 管理员和个人用户权限隔离。
- 保险箱主密码和恢复密钥流程。
- 浏览器端加密和服务端密文保存。
- 用户数据隔离。
- 手动新增、查看、编辑、归档和删除账密。
- 动态字段及字段排序。
- 平台、渠道和状态管理。
- 系统模板和私人模板。
- 搜索、筛选和复制。
- 2FA 动态验证码生成。
- 自动锁定和退出设备。
- 管理后台的用户、邀请码和模板管理。
- 加密备份导出和恢复。

### 14.2 P1：第一版增强

- 粘贴文本自动识别字段。
- 回收站和恢复。
- 到期时间提醒。
- 新设备或异常登录提醒。
- 卡片和列表两种展示模式。

### 14.3 P2：后续规划

- Markdown、CSV 和 Excel 批量导入。
- 重复记录检测。
- 附件加密保存。
- 离线访问能力。
- 浏览器扩展和自动填充。
- 用户之间受控共享账密。
- 开放注册及服务商业化能力。

## 15. 第一版验收标准

满足以下条件时，可以认为第一版达到基本可用状态：

1. 两名不同个人用户注册后，只能看到各自创建的账密记录。
2. 管理员可以管理用户和模板，但无法读取任何用户账密明文。
3. 用户可以使用完全不同的字段结构保存不同渠道的账号资料。
4. 用户可以把一条记录的字段结构保存为私人模板，并再次使用。
5. 系统模板修改或停用后，不影响用户已经保存的记录。
6. 密码、2FA 密钥和恢复密钥不会出现在服务器日志中。
7. 数据库中的账密内容为密文，无法通过直接查询数据库得到明文。
8. 用户可以在两台设备登录并访问同步后的账密数据。
9. 用户退出或自动锁定后，必须重新输入主密码才能查看敏感内容。
10. 用户可以导出加密备份，并在验证环境中成功恢复。
11. 修改记录请求中的用户标识不能绕过权限访问其他用户数据。
12. 管理员入口和个人用户入口具有独立路由及角色校验。
13. 注册时手机号和用户名均不能重复，密码与确认密码不一致时不能创建账户。
14. 第一版注册流程不依赖短信验证码，并且系统不得把未验证手机号当作可信找回凭证。

## 16. 待确认事项

以下内容需要在进入详细设计前进一步确认：

- 第一版是否正式采用邀请码注册，还是直接开放注册。
- 个人用户登录使用用户名、手机号，还是两者都支持。
- 第一版采用何种账户找回方式；在手机号未经验证的情况下，不能直接通过手机号重置密码。
- 后续启用短信验证时选择哪一家短信服务商。
- 正式部署使用的域名、服务器和数据库环境。
- 自动锁定的默认时间。
- 回收站保留期限。
- 是否在第一版提供粘贴文本自动识别。
- 加密备份是否同时支持本地文件和云端历史版本。
- 第一版预计用户数量和单用户平均记录数量。

## 17. 产品原则总结

- 管理员管理系统，不管理用户的秘密。
- 用户数据默认私有，任何接口都不能依赖前端进行权限隔离。
- 账号来源和字段结构可以变化，数据模型不得被固定字段限制。
- 模板帮助用户提高录入效率，但不能控制或破坏已有记录。
- 先保证安全、隔离和可恢复，再扩展批量导入、共享和商业化功能。
